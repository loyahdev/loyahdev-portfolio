"use client";

import React from "react";

const FloatingBubbles = () => {
  return (
    <div className="fixed inset-0 -z-10 pointer-events-none bg-animated-gradient bg-[length:200%_200%] animate-gradient-shift" />
  );
};

export default FloatingBubbles;
